

import { requireNativeComponent } from 'react-native';

// requireNativeComponent automatically resolves this to "SampleViewManager"
module.exports = requireNativeComponent('SampleViewManager', null);